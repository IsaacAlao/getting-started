          echo What is in this directory? && ls -la
          echo
          echo Is Git installed?
          git --version
          echo What about build tools?
          echo "NPM VERSION"
          npm --version
